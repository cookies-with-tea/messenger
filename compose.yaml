version: '3.8'

services:
  web:
    container_name: rust-messenger
    build:
      context: ./
      dockerfile: Dockerfile
    env_file: .env
    ports:
      - "${APP_PORT}:${APP_PORT}"
    volumes:
      - ./:/opt/rust-messenger/
    depends_on:
      - db

  db:
    image: postgres:16.0
    container_name: rust-messenger-db
    volumes:
      - postgres_data:/var/lib/postgresql/data
    env_file: .env
    ports:
      - "${POSTGRES_PORT}:${POSTGRES_PORT}"
    healthcheck:
      test: pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}
      interval: 10s
      timeout: 15s
      retries: 5


volumes:
  postgres_data:
